<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.BoardMapper">

    <select id="findAll" resultType="com.example.demo.dto.BoardDto">
        SELECT id, title, content, writer, reg_date as regDate 
        FROM board 
        ORDER BY id DESC
    </select>

    <insert id="save" parameterType="com.example.demo.dto.BoardDto">
        INSERT INTO board (title, content, writer)
        VALUES (#{title}, #{content}, #{writer})
    </insert>

    <select id="findById" resultType="com.example.demo.dto.BoardDto">
        SELECT id, title, content, writer, reg_date as regDate 
        FROM board 
        WHERE id = #{id} 
    </select>

    <delete id="delete">
          DELETE FROM board WHERE id = #{id}
    </delete>

    <update id="update">
        UPDATE board 
        SET title = #{title}, 
            content = #{content}, 
            writer = #{writer}
        WHERE id = #{id}  
    </update>
</mapper>